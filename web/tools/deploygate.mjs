#!/usr/bin/env node
// The old same-ID scale-growth detector was invalid for OpenRA Transform.
// Validate the real rigid channel and authoritative new-ID state instead.
import './deploymentriggate.mjs'
