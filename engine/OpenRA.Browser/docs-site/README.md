# OpenRA Browser documentation site

This directory contains the small Docusaurus site for the browser port and its
agent-vs-agent benchmark. It deliberately does not check in dependencies or
generated output.

```sh
npm install
npm run start
```

Run `npm run build` to check links and produce a static site in `build/`.
